{
  "name": "@repo/nft-contracts",
  "type": "module",
  "main": "./index.ts",
  "module": "./index.ts",
  "export": {
    "./": "./index.ts"
  },
  "scripts": {
    "cli": "bun ../web3/bin/cli.ts",
    "compile": "bun cli compile --network testnet",
    "deploy": "bun cli deploy --network testnet",
    "test": "bun test",
    "compile:watch": "fd -e .ral | entr -r bun run compile",
    "test:watch": "fd -e .ts | entr -r bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@repo/web3": "workspace:*"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
